# Artefatos locais / scripts temporários
.tmp-favicon
.git
.github
.vscode
.idea

# Dependências e builds locais (reinstalados no container)
node_modules
.next
out

# Landing separada — não entra no build do app principal
site/node_modules
site/.next

# Capacitor Android — só para build local do app nativo
android
capacitor-www

# Env e uploads locais
.env
.env.*
!.env.example
public/uploads/*
!public/uploads/.gitkeep

# Testes e docs de dev
**/*.test.ts
**/*.spec.ts
coverage
